# Data generated by the pipeline (scripts/, just convert/enrich) — kept as
# minified single-line JSON for smaller file size / faster load. Prettier
# would pretty-print them, massively bloating the files with no benefit.
public/data/
